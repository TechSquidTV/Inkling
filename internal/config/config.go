package config

const (
	// AppName is the display name of the application
	AppName = "Inkling"

	// ServiceName is the name used for telemetry and tracing
	ServiceName = "inkling"

	// APITitle is the title shown in the OpenAPI documentation
	APITitle = "Inkling API"

	// APIKeyPrefix is the prefix for generated API keys
	APIKeyPrefix = "ink_"

	// DefaultDBName is the default database filename
	DefaultDBName = "inkling.db"
)
